<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.omidrezabagherian.aniclass">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AniClass">
        <activity android:name=".SettingStudentActivity"></activity>
        <activity android:name=".FollowedStudentActivity" />
        <activity android:name=".MainStudentActivity">
        </activity>
        <activity android:name=".SignupTeacherActivity" />
        <activity android:name=".SignupStudentActivity" />
        <activity android:name=".LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>